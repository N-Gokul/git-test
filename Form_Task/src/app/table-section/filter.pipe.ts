// import { Pipe, PipeTransform } from '@angular/core';
// import { formData } from '../type';
// @Pipe({
//   name: 'dataFilter',
// })
// export class dataFilter implements PipeTransform {
//   transform(data: formData[], searchTerm: string) {
//     if (!data || !searchTerm) {
//       return data;
//     }
//     return data.filter(
//       (d) => d.Name.toLowerCase().indexOf(searchTerm.toLowerCase()) !== -1
//     );
//   }
// }
